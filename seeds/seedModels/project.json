{
  "filename": "project.json",
  "name": "Project",
  "properties_seeds": [
    {
      "project_number": "{{random.number}}"
    },
    {
      "description": "{{commerce.productName}} - {{random.words}}"
    },
    {
      "programId": "{{random.number({\"min\":1, \"max\":20})}}"
    }
  ]
}
