{
  "filename": "concept-requisition.json",
  "name": "Concept_Requisition",
  "properties_seeds": [
    {
      "conceptId": "{{random.number({\"min\":1, \"max\":20})}}"
    },
    {
      "description": "{{random.words}} - {{random.words}}"
    },
    {
      "quantity": "{{random.number({\"min\":1, \"max\":200})}}"
    },
    {
      "requisitionId": "{{random.number({\"min\":1, \"max\":20})}}"
    },
    {
      "unit": "{{commerce.productMaterial}}"
    }
  ]
}
