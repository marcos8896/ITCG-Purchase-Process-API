{
  "filename": "requisition.json",
  "name": "Requisition",
  "properties_seeds": [
    {
      "date": "{{date.recent}}"
    },
    {
      "action": "{{random.words}} - {{random.words}}"
    },
    {
      "providerId": "{{random.number({\"min\":1, \"max\":20})}}"
    },
    {
      "budget_keyId": "{{random.number({\"min\":1, \"max\":20})}}"
    },
    {
      "boss_departmentId": "{{random.number({\"min\":1, \"max\":2})}}"
    }
  ]
}
